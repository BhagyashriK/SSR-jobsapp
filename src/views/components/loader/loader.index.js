import React from "react";
import { Wrapper } from "./loader.style";

// Loading componet, can be enhanced with spinner & loading icon
const Loader = props => {
  return <Wrapper>Loading...</Wrapper>;
};

export default Loader;
